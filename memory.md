# Boaz Health project memory

This is a small, repo-local decision and handoff ledger for Claude Code and Codex. It contains **no personal profile, HealthKit record, credential, or private host secret**. It does not replace current source, Git diffs, runtime checks, or an acceptance report. Update only when a decision or verified status changes, with date, evidence pointer, and owner/next action.

## 2026-09-18 — current handoff

| Kind | Entry | Evidence / next action |
|---|---|---|
| Legacy proposal, not app contract | CLI context-window protocol and attachment/archive ideas are not adopted by this Health app. The Tokyo route, encrypted storage choice, native metrics identity and restore remain deployment targets; no fixed LUKS choice or 4,096-byte metric-label guard is implemented. | `design.md` §4–5, `Server/src/projection.rs`, `docs/TOKYO_PREFLIGHT.md`; do not promote proposals to source facts or release requirements. |
| Verified in source | The repo has a SwiftUI/HealthKit iOS app, protected local SQLite ledger/outbox, and a Rust receiver with a separate SQLite ledger and native-metrics projection checks. | `boazapp/`, `Server/src/`, `boazapp/Core/Database/Schema.sql`, `Server/src/schema.sql`; re-read the current worktree before relying on this summary. |
| Decision | Health access, private pairing, and cloud-upload consent are separate. Do not auto-upload on pairing or use empty HealthKit results as permission proof. | `boazapp/App/BoazCoordinator.swift`, `boazapp/Core/Network/SyncEngine.swift`; retain synthetic and device acceptance cases. |
| Decision | Tokyo SQLite is cloud-record authority; VictoriaMetrics is derived and must be native, not Docker. No Meilisearch/Qdrant/Neo4j/GraphRAG/LangGraph/LLM for deterministic ingestion. | `Server/README.md`, `Server/src/projection.rs`, `design.md`; justify any future change with measured benefit and privacy gate. |
| Dated observation, not current guarantee | Tokyo preflight found Docker-proxy metrics, missing separate encrypted health storage, no Tailscale Serve route and no deployed receiver. | `docs/TOKYO_PREFLIGHT.md`; operator must refresh read-only checks and obtain an explicit cutover decision. |
| Open acceptance | Real signed iPhone/Apple Watch flow, locked-phone behavior, private Tokyo ingest, native VM readback, encrypted restore, erasure/backup expiry, rollback, scale/cost measurement. | `docs/TEST_CASES.md`, `design.md` §8; keep `AI-NATIVE GATE: INTERCEPT`. |
| Privacy gap to test | iOS source now sets backup exclusion on the protected database directory; the effective database/WAL/SHM behavior is not yet demonstrated on a signed device. | `boazapp/Core/Database/BoazLocalDatabase.swift`, `docs/TEST_CASES.md`; verify before release. |
| Derived-metrics limit | Same-millisecond readings can collapse to one projected metric point; Tokyo SQLite retains distinct events. | `Server/src/projection.rs`; never equate metric point count with Health event count. |
| Acceptance evidence | The dated test specification, acceptance report and retained evidence directory now exist. Local passing checks do not close the physical-device or Tokyo gates. | `docs/TEST_CASES.md`, `docs/ACCEPTANCE_REPORT_2026-09-18.md`, `docs/evidence/2026-09-18/`; verify the recorded source hashes before reusing results. |

## 2026-09-18 — iOS storage migration decision

| Kind | Entry | Evidence / next action |
|---|---|---|
| Decision | Adopt GRDB.swift 7.11.1 `DatabasePool` on the existing phone SQLite path, WAL with an explicitly verified FULL writer, and transactional schema v2 change-clock migration. Preserve exact outbox bytes and separate cloud consent. | `design.md` §6, `boazapp/Core/Database/BoazLocalDatabase.swift`, `Schema.sql`, Xcode generator and resolved package; the [current synthetic verification](docs/GRDB_CLIENT_VERIFICATION_2026-09-18.md) passed, but device/host acceptance is still open. |
| Recovery boundary | A failed v0/v1 migration leaves the former ledger intact. After any v2 write, restore an old binary only if it is v2-compatible; never overwrite acknowledged data with a stale v1 snapshot. | `docs/TEST_CASES.md` BLD-06/07 and isolated migration/recovery fixtures; signed iPhone protection and Tokyo restore remain open. |

## 2026-09-19 — P0 authority and recovery decisions

| Kind | Entry | Evidence / next action |
|---|---|---|
| Governance decision | `AGENTS.md` is the repository contract; `CLAUDE.md`, `agent.md` and root `prd.html` are thin compatibility entries. `docs/prd.html` is the sole PRD. | `scripts/verify_doc_governance.py`; keep duplicated contracts from reappearing. |
| Client decision | Keep the existing phone `health.sqlite`; do not create a second phone database. Only a legal erasure `complete` rotates `device_id`, once per erasure, clears old credentials and requires a fresh pairing plus upload consent. | `boazapp/App/BoazCoordinator.swift`, `TokyoCloudGateway.swift`, client tests; signed-device/Keychain behavior remains open. |
| Server identity decision | Health DB is `BZHR` (`application_id=0x425A4852`) v2. Control DB was `BZHC` (`application_id=0x425A4843`) v1 at this checkpoint; P0-R later introduced v2, below. Storage opens only through validated paths and explicit `init-storage`, `migrate-storage`, `verify-storage` commands. | `Server/src/database.rs`, `Server/src/control.rs`, `Server/src/main.rs`; re-run exact migration/path tests before relying on this entry. |
| Cross-store decision | Never claim cross-SQLite atomicity. Commit revocation/erasure control intent first, reject tombstoned credentials, then reconcile the health ledger and metrics idempotently. | `Server/src/control.rs`, `Server/src/lib.rs`, `docs/TEST_CASES.md` ERA/BAK/OPS cases. |
| Projection decision | `metrics_current` is receipt evidence for one verified projection generation/mapping version at `projected_at`; generation drift requires rebuild and old metric evidence is not current. Label hashing is not implemented. | `Server/src/schema.sql`, `Server/src/projection.rs`; native Tokyo readback remains a release gate. |
| Encryption boundary | The Linux verifier recognizes dm-crypt only. Production backup/prune require a physically verified dm-crypt backup mount on a device distinct from both live databases; a flag-only claim is rejected before mutation. Provider-managed encryption, actual Tokyo mount identity, independently recoverable control storage and off-host expected-head custody remain unverified. | `Server/src/main.rs`, `Server/src/projection.rs`, `design.md` §8; synthetic tests do not attest a host mount. |
| Recovery progress | Control publication and paired ingress/erasure now use locks; legacy migration preclassifies read-only and can resume an exact control seed prefix. Health backup pruning checks file/manifest/hash/control-event inventory before expiry. Staged control/health recovery replays all applicable tombstones, including previously verified erasures. | `Server/src/control.rs`, `Server/src/lib.rs`, `Server/src/main.rs`, `Server/src/recovery.rs`; 69 local Rust tests pass on the refreshed worktree. |
| Restore boundary | `backup-control`, `restore-control` and `restore-health --staging-path` now exist. Restore stages from verified artifacts even when a live database is corrupt; it still requires a valid live directory and exclusive offline lifecycle lock. `restore-health` deliberately exits nonzero at `projection_rebuild_required`: no isolated native VM rebuild/readback, completed epoch, live cutover or second-generation Tokyo drill has been accepted. | `Server/src/recovery.rs`, `Server/src/main.rs`, `docs/TEST_CASES.md` BAK-04/05/06; keep upload closed. |
| Evidence status | The current-cycle register contains 77 cases with a dated [2026-09-19 record](docs/evidence/2026-09-19/README.md); local suite success does not pass device/Tokyo/restore cases. Historical 2026-09-18 evidence stays immutable. | `docs/TEST_CASES.md`; preserve the source-manifest limit and record a new result for later changes. |

## 2026-09-19 — historical P0-R checkpoint, superseded by P0-R2 below

| Kind | Entry | Evidence / next action |
|---|---|---|
| Version decision | The control database now declares `BZHC` v2 and supports a classified v1→v2 migration. Historical v1 event hashes remain versioned instead of being recalculated. Old binaries that understand only v1 are not a safe rollback after v2 writes. | `Server/src/control.rs`, `Server/src/main.rs`; verify migration and all call paths on the final frozen source. |
| Backup deletion decision | Record a snapshot/hash/name-bound `backup_delete_intent` before deleting managed artifacts. Resume only when physical inventory matches that intent; otherwise fail closed and require operator investigation. A successful local unit test is not process-kill or scheduler evidence. | `Server/src/control.rs`, `Server/src/main.rs`, `Server/tests/api.rs`; run crash-boundary injection in isolated Linux. |
| Recovery boundary | Coordinator journal/activation, isolated native staging VM, deterministic projection oracle and exact export are library capabilities. The CLI conditionally takes the coordinator lock before legacy path resolution and refuses to serve/write when a journal or active-set is present; `BOAZ_HEALTH_COORD_REQUIRED=1` refuses a missing coordinator. `restore-health` remains deliberately nonzero at `projection_rebuild_required`; completed activation, zero-loss post-snapshot accounting, external head custody, live-VM restart/readback and second-generation recovery are not accepted. | `Server/src/activation.rs`, `Server/src/recovery.rs`, `Server/src/projection.rs`, `Server/src/main.rs`, [P0-R evidence](docs/evidence/2026-09-19-p0r/README.md); retain `AI-NATIVE GATE: INTERCEPT`. |
| Release boundary | Local/macOS synthetic checks cannot establish dm-crypt, separate Linux network namespace, native VM persistence, signed iPhone/Watch behavior or Tokyo rollback. Do not enable upload, deploy or publish based on this checkpoint. | [P0-R test annex](docs/TEST_CASES.md#p0-r-disaster-recovery-acceptance-annex); obtain an approved isolated Linux host and later a separate Tokyo release decision. |

## 2026-09-19 — P0-R2 safety checkpoint, not activation acceptance

| Kind | Entry | Evidence / next action |
|---|---|---|
| Control migration | Current `BZHC` layout is v3. The classified v1/v2 event table is retained as an immutable prefix, with future events in an append-only suffix; the earlier v2 entry above is historical. No `DROP TABLE` recovery shortcut is accepted. | `Server/src/control.rs`; run final migration/future-version/unknown-layout tests on a frozen source. |
| Startup safety | The coordinator is mandatory before storage-path resolution. Only explicit offline `bootstrap-coordinator` can initialize it; missing/incomplete coordinator, journal or active pointer blocks ordinary commands. Even an `ActiveVerified` local record remains blocked until independent custody and restarted live VM export are verified at startup. | `Server/src/main.rs`, `Server/src/activation.rs`; fail-closed is not successful activation or a deployed receiver. |
| Acknowledged-write boundary | An independently initialized acknowledgement journal can retain exact prepared batch bytes and confirmed receipt/pairing facts. Missing journal denies receipt and pairing paths; a snapshot sequence alone cannot prove all post-snapshot legitimate writes. Restore coverage/replay functions are library code, not a completed cutover. | `Server/src/ack_journal.rs`, `Server/src/lib.rs`, `Server/src/recovery.rs`; establish adopted baseline, independent encrypted volume, crash tests and complete replay inventory. |
| Custody boundary | SSH `read/reserve/CAS` client and forced-command server code has synthetic local tests. Its root must belong to an independent operator/domain; no real host, authenticated external head advancement, production deployment or startup integration is evidenced. A local expected-head file remains only a fixture. | `Server/src/custody.rs`, `Server/src/main.rs`; independently provision and test only on an authorized isolated Linux host. |
| Release gate | Native isolated VM rebuild and full export binding, live-port restart/readback, controlled pointer activation, second-generation recovery and 10k/100k Linux performance evidence remain open. Do not enable upload or infer RPO=0 from local unit tests. | `docs/TEST_CASES.md` P0-R2 annex; `AI-NATIVE GATE: INTERCEPT`. |

## 2026-09-19 — P0-R2.1 synthetic source checkpoint

| Kind | Entry | Evidence / next action |
|---|---|---|
| Custody decision | One v2 off-host revision binds control checkpoint, full acknowledgement-journal head and immutable generation-zero baseline digest. Routine control events and externally successful batch/pairing receipts require a matching independent CAS; local committed facts alone do not authorize success. | Server/src/custody.rs, control.rs, ack_journal.rs, lib.rs; independently administered SSH host and full crash-boundary drill remain unverified. |
| Adoption decision | The offline adopt-active-set command accepts only a fresh genesis store and publishes adopted-unactivated.json after managed snapshot and independent baseline confirmation. It does not publish an active-set pointer or enable serve. Historical pre-journal data is not retroactively RPO=0. | Server/src/adoption.rs, activation.rs, main.rs; positive Linux/dm-crypt/off-host exercise remains BLOCKED. |
| Performance limit | The v2 custodian rechecks full immutable history per request; cumulative per-batch cost may grow superlinearly. No world-class throughput or 15% regression result is claimed without fixed-host 10k/100k measurements. | Server/src/custody.rs; establish a correct same-host baseline before optimizing. |
| Release gate | Production serve remains intentionally closed pending native VM full rebuild, live-port restart/readback, safe activation, second-generation recovery and independent device/Tokyo acceptance. | docs/evidence/2026-09-19-p0r21/README.md; AI-NATIVE GATE: INTERCEPT. |

## 2026-09-19 — P0-R2.1a local safety checkpoint

| Kind | Entry | Evidence / next action |
|---|---|---|
| Managed-backup decision | A governed backup creation, delete intent or deletion now freezes the managed root, exact file/manifest hashes and Unix identities in its reservation-bound intent; backup expiry freezes the complete inventory and erasure request time. Restart rechecks proof before local convergence and before off-host CAS; missing or changed evidence remains unresolved. Old intents without proof are not auto-promoted. | `Server/src/control.rs`, `Server/src/main.rs`, `Server/tests/control_process.rs`; isolated Linux and all persistence-boundary kill tests remain separate gates. |
| Custody integrity decision | The v2 custodian now requires a history seal binding each full reservation and successor. Mirror → seal → head preserves exact-CAS retry; old unsealed v2 history fails closed and needs independent offline review rather than automatic migration. A same-domain seal is not proof against coordinated rollback of the entire custodian. | `Server/src/custody.rs`; provision a separately administered host and monotonic external ownership before release. |
| Crash-boundary correction | Before a health backup, converge already-confirmed erasure intents so the snapshot cannot capture pending-to-delete rows. Backup pruning pins the managed directory and file identities and rejects a substituted file before confirming deletion. Matching custody temporary files can resume after a process kill; unknown legacy temporary files remain fail-closed. | `Server/src/main.rs`, `Server/src/custody.rs`, `Server/tests/api.rs`, `Server/tests/control_process.rs`; same-UID races and independent Linux remain open. |
| Cost and release boundary | Every v2 forced request still scans the full history. Source inspection indicates superlinear cumulative cost for per-batch confirmations, but no fixed Linux 10k/100k p95/p99 or resource baseline exists. Production `serve`, VM activation and real upload remain closed. | `docs/evidence/2026-09-19-p0r21a/README.md`, `docs/TEST_CASES.md`; `AI-NATIVE GATE: INTERCEPT`. |

## 2026-09-19 — P0-R2.1b local hardening checkpoint

| Kind | Entry | Evidence / next action |
|---|---|---|
| Full custody binding | Governed control writes now compare the complete v2 state with the local control head, confirmed acknowledgement-journal prefix, baseline digest and derived revision. A matching control head with changed journal/baseline/revision is not enough to proceed. | `Server/src/control.rs`, `Server/src/ack_journal.rs`, `Server/src/lib.rs`; repeat with an independently administered SSH host and durable fault injection. |
| Transport limit and adoption retry (historical checkpoint) | At P0-R2.1b the SSH I/O loop had a 30-second deadline but spawn and cleanup were outside it; P0-R2.1c changes that implementation below. Genesis adoption may retry only its exact one-event snapshot/baseline transition and authenticated successor readback; empty business tables with nonempty `sqlite_sequence` are rejected as prior history. | `Server/src/custody.rs`, `Server/src/adoption.rs`, `Server/src/main.rs`; independent Linux/SSH/dm-crypt witness and zero loss for older ledgers remain unverified. |
| Tombstone crash boundary | Local child-process tests force-kill revoke and erasure publication after reserve, before CAS and after CAS, then require the same intent to converge and the token tombstone to remain. | `Server/tests/control_process.rs`; other fsync boundaries, independent SSH, same-UID races, performance and second-generation restore remain open. `serve` and real upload remain disabled; `AI-NATIVE GATE: INTERCEPT`. |

## 2026-09-19 — P0-R2.1c acceptance decision, not host evidence

| Kind | Entry | Evidence / next action |
|---|---|---|
| Trust boundary | Current code fixes `/usr/bin/ssh` and requires root-controlled, non-symlink, non-group/world-writable key/known-host files and ancestors; receiver must only read them, not replace them. This is a source/local-test constraint, not a real Linux process or privileged-replacement attestation. | `Server/src/custody.rs`, `design.md` §6.2; run adversarial independent Linux/SSH tests with recorded ownership and actual consumed identity. |
| Unknown outcome | The 30-second budget now starts before spawn and failed child cleanup bounds the caller-side reap attempt. Uninterruptible OS work and an HTTP batch's end-to-end confirmation remain unbounded/unmeasured. A timeout or lost CAS response is unresolved until exact original-ID/successor readback. A fail-first false-CAS test led to a separate exact remote read before the pending acknowledgement intent is cleared. | `Server/src/custody.rs`, `Server/src/lib.rs`, `Server/tests/api.rs`, `docs/TEST_CASES.md` P0-R2.1c; do not claim an absolute bound from local child tests. |
| Independent rollback and cost | A same-domain history/seal/head rollback needs an independently owned monotonic witness; the current forced command scans full history per request. Approved dual-host/independent-domain evidence and pre-frozen p95/p99/resource targets are absent; 10k/100k three-run acceptance remains `BLOCKED`. | `Server/src/custody.rs`, `design.md` §6.2; measure on one approved Linux setup before considering any checkpoint optimization. |
| Release gate | P0-R2.1c source-level repairs cannot themselves authorize `serve`, VM activation, Tokyo connection or upload. The operator template deliberately omits a `verify-storage` pre-start gate so exact pending-custody reconciliation can run first. | `Server/deploy/boaz-health.service.example`, `Server/README.md`; `AI-NATIVE GATE: INTERCEPT`. |

## 2026-09-19 — P0-R2.1d local false-success checkpoint

| Kind | Entry | Evidence / next action |
|---|---|---|
| Same-domain rollback | A complete rollback of custodian history, seal and head to an older internally consistent state passes the v2 local verifier. A separately owned monotonic witness binding the full custody tuple is still a design requirement, not an implemented or accepted service. | `Server/src/custody.rs`, `design.md` §6.3; obtain approved independent owner, identity, domains and fault-injection environment. |
| False-success fixes | Control publication retains its original pending intent until a separate exact custody read confirms CAS. Erasure HTTP reads require settled control/custody facts and exact intent/metrics/backup timestamps; a health-only row cannot assert completion. | `Server/src/control.rs`, `Server/src/lib.rs`, focused red/green tests and new P0-R2.1d evidence; these are synthetic source checks only. |
| Backup freeze | A local failing race test proved the projection worker could erase health rows while a backup held the health-operation lock. The worker now takes that lock for a projection pass and retries if busy. This may increase confirmation/backup latency; Linux measurement is open. | `Server/src/projection.rs`, `Server/tests/api.rs`; independent process-level race and cost acceptance remain blocked. |
| Release decision | Independent Linux/witness crash matrix, fixed-host confirmation cost, native VM activation and second-generation recovery remain unverified. | `docs/TEST_CASES.md` P0-R2.1d annex; `AI-NATIVE GATE: INTERCEPT`. |

## Maintenance rules

- Start each task with `git status --short --branch`, then verify relevant files and tests; this ledger may be stale.
- Record only durable decisions and unresolved gates here. Put implementation details in `design.md`, product requirements in `docs/prd.html`, usage in `README.md`, and executed test receipts in a real dated acceptance report.
- If the physical source or live host conflicts with an entry, correct the entry after preserving evidence; never edit source or deploy solely to make this ledger look consistent.
